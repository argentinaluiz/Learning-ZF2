<h1>Roles</h1>

<?= $this->paginationControl($this->data, 'Sliding', 'partials/paginator') ;?>


<a href="<?= $this->url('acl-admin/default',['controller' => 'roles', 'action'=>'new']); ?>" title="Novo Roles" class="btn alert-success">Novo</a>
<br>
<br>
<table class="table table-condensed">
    <tr>
        <th>ID</th>
        <th>Nome</th>
        <th>Herança</th>
        <th>Admin?</th>
        <th>Ação</th>
    </tr>
    <?php foreach ($this->data as $entity) : ?>
        <tr>
            <td><?= $entity->getId(); ?></td>
            <td><?= $entity->getName(); ?></td>
            <td><?= $entity->getParent() ? $entity->getParent() : '###'; ?></td>
            <td><?= $entity->getIsAdmin()? 'Sim' : 'Não'; ?></td>
            <td>
                <a href="<?= $this->url('acl-admin/default',['controller' => 'roles','action'=>'edit', 'id' => $entity->getId()]); ?>" title="Edição de Role" class="btn alert-info">Editar</a>
                <a href="<?= $this->url('acl-admin/default',['controller' => 'roles','action'=>'delete', 'id' => $entity->getId()]); ?>" title="delete Role" class="btn alert-danger">Delete</a>
            </td>

        </tr>
    <?php endforeach; ?>


</table>

<div class="">
    <?= $this->paginationControl($this->data, 'Sliding', 'partials/paginator') ;?>
</div>